"use strict";

const timeContainer = document.querySelector(".timeContainer");
const timerControlsContainer = document.querySelector(".timerControlsContainer");

const timer = new Timer(timeContainer, timerControlsContainer);